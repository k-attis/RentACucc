<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RentACucc.View.CuccokLap">

  <ContentPage.ToolbarItems>
    <ToolbarItem x:Name="newCucc" Text="Új Cucc"/>
  </ContentPage.ToolbarItems>

  <ListView x:Name = "cuccokLista">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <StackLayout Orientation="Horizontal">
            <Label Text="{Binding SN}" VerticalTextAlignment="Center" HorizontalOptions="FillAndExpand"/>
            <Label Text="{Binding Nev}" VerticalTextAlignment="Center" HorizontalOptions="FillAndExpand"/>
            <Switch IsToggled="{Binding Kolcsonozve}" HorizontalOptions="FillAndExpand"/>
          </StackLayout>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>

</ContentPage>